import setSettings from "api/setSettings";
import { useMutation } from "react-query";

const useSetSettings = () => {
    // return useMutation(setSettings);
    return true;
}

export default useSetSettings;